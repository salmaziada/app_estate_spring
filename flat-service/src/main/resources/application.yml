
server:
  port: 9092

spring:
  application:
    name: flastservice

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/flastservice?serverTimezone=UTC
     username:root
     password:root
    nitialization-mode: always
  properties:
    hibernate:
      dialect: org.hibernate.dialect.MySQL57Dialect
      ddl-auto: update
